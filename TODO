- Examples
  - HTML templates.
  - Static assets.
  - Sessions.
- Params
  - *Targetify* HTTP parameters.
  - Params: Give preference to post variables when in query string and form.
  - Support Multipart forms.
- Routing
  - Named captures.
  - Ixed instance for `Target`.
  - Property testing for `Prism`s laws matching lens.
- Monoid instance for `Request` and `Response`?
- Monoid instance for `Payload`
- `slashRedirect` shouldn't convert results, it should work on `Match` only.
- External packages
  - Monoidal matching.
  - WAI handler.
