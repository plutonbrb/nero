- Examples
  - HTML templates
  - Static assets
  - Sessions
- Params
  - Unify `Param` and `Match` API.
  - Params: Give preference to post variables when in query string and form.
  - Support Multipart forms
- Routing
  - New operator embedding `target` within `^?`, or look for a clean way to
    convert matched results into targets.
  - Capture `Int`s and `Floats`.
  - Named captures.
- Monoid instance for `Request` and `Response`?
